# Neural Aquarium - Visual Overview

## Application Layout

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          Neural Aquarium                                 │
│                     AI-Powered Neural DSL Builder                        │
└─────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────┬─────────────────────────────────────┐
│                                   │  ╔═══════════════════════════════╗  │
│                                   │  ║  AI Assistant Sidebar         ║  │
│        Main Workspace             │  ║  ┌─────────────────────────┐ ║  │
│                                   │  ║  │ 🌐 Language: English ▾  │ ║  │
│   ┌─────────────────────────┐    │  ║  └─────────────────────────┘ ║  │
│   │                         │    │  ║                               ║  │
│   │   Model Workspace       │    │  ║  ┌─────────────────────────┐ ║  │
│   │                         │    │  ║  │ 👤 You:                 │ ║  │
│   │   Applied DSL Code      │    │  ║  │ Create a CNN for MNIST  │ ║  │
│   │   Displays Here         │    │  ║  └─────────────────────────┘ ║  │
│   │                         │    │  ║                               ║  │
│   └─────────────────────────┘    │  ║  ┌─────────────────────────┐ ║  │
│                                   │  ║  │ 🤖 Assistant:           │ ║  │
│                                   │  ║  │ I've created a CNN...   │ ║  │
│                                   │  ║  └─────────────────────────┘ ║  │
│                                   │  ║                               ║  │
│                                   │  ║  ┌─────────────────────────┐ ║  │
│                                   │  ║  │ Generated DSL Code      │ ║  │
│                                   │  ║  │ ✏️ Edit | 📋 Copy      │ ║  │
│                                   │  ║  │ ⬇️ Download | ✓ Apply  │ ║  │
│                                   │  ║  │                         │ ║  │
│                                   │  ║  │ network MyModel {       │ ║  │
│                                   │  ║  │   input: (28, 28, 1)   │ ║  │
│                                   │  ║  │   layers:               │ ║  │
│                                   │  ║  │     Conv2D(32, ...)     │ ║  │
│                                   │  ║  │   ...                   │ ║  │
│                                   │  ║  │ }                       │ ║  │
│                                   │  ║  └─────────────────────────┘ ║  │
│                                   │  ║                               ║  │
│                                   │  ║  ┌─────────────────────────┐ ║  │
│                                   │  ║  │ Type your message...    │ ║  │
│                                   │  ║  │                         │ ║  │
│                                   │  ║  └────────────────────[Send]┘ ║  │
│                                   │  ║                               ║  │
│                                   │  ║  [Reset Conversation]         ║  │
│                                   │  ╚═══════════════════════════════╝  │
└───────────────────────────────────┴─────────────────────────────────────┘
```

## Component Hierarchy

```
App
└── AIAssistantSidebar
    ├── LanguageSelector
    │   └── <select> dropdown
    │
    ├── ChatInterface
    │   ├── MessageBubble (multiple)
    │   │   └── ReactMarkdown
    │   ├── Suggestions (conditional)
    │   └── InputForm
    │       ├── <textarea>
    │       └── <button>
    │
    ├── DSLCodeViewer (conditional)
    │   ├── Viewer Header
    │   │   └── Action Buttons
    │   ├── Code Container
    │   │   ├── <pre><code> (display mode)
    │   │   └── <textarea> (edit mode)
    │   └── Viewer Footer
    │
    └── Sidebar Actions
        └── Reset Button
```

## Data Flow Diagram

```
┌─────────────┐
│    User     │
└──────┬──────┘
       │ Types message
       ▼
┌─────────────────────────────────────────────┐
│          ChatInterface                      │
│  ┌──────────────────────────────────────┐  │
│  │  Message Input                       │  │
│  └──────────────────────────────────────┘  │
└──────┬──────────────────────────────────────┘
       │ onSendMessage()
       ▼
┌─────────────────────────────────────────────┐
│       AIAssistantSidebar                    │
│  - handleSendMessage()                      │
│  - Updates messages state                   │
└──────┬──────────────────────────────────────┘
       │ Calls AIService.chat()
       ▼
┌─────────────────────────────────────────────┐
│          AIService                          │
│  - HTTP POST to /api/ai/chat                │
└──────┬──────────────────────────────────────┘
       │ axios request
       ▼
┌─────────────────────────────────────────────┐
│       Flask Backend API                     │
│  /api/ai/chat endpoint                      │
└──────┬──────────────────────────────────────┘
       │ Calls assistant.chat()
       ▼
┌─────────────────────────────────────────────┐
│      NeuralAIAssistant                      │
│  - Process language (MultiLanguageSupport) │
│  - Extract intent (NaturalLanguageProcessor)│
│  - Generate DSL (DSLGenerator)              │
└──────┬──────────────────────────────────────┘
       │ Returns response + DSL code
       ▼
┌─────────────────────────────────────────────┐
│       Backend API Response                  │
│  {response, dsl_code, intent, success}      │
└──────┬──────────────────────────────────────┘
       │ JSON response
       ▼
┌─────────────────────────────────────────────┐
│          AIService                          │
│  - Parses response                          │
└──────┬──────────────────────────────────────┘
       │ Returns AIResponse
       ▼
┌─────────────────────────────────────────────┐
│       AIAssistantSidebar                    │
│  - Updates messages                         │
│  - Updates currentDSL                       │
└──────┬──────────────────────────────────────┘
       │
       ├──────────────────┬──────────────────┐
       ▼                  ▼                  ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ MessageBubble│  │DSLCodeViewer │  │    User      │
│  (displays)  │  │  (displays)  │  │   (sees)     │
└──────────────┘  └──────────────┘  └──────────────┘
```

## UI Interaction Flow

```
1. User Opens Sidebar
   └─> Sidebar slides in from right
       └─> Shows welcome message
           └─> Shows language selector
               └─> Shows suggestion prompts

2. User Selects Language
   └─> Dropdown changes
       └─> Language state updates
           └─> Confirmation message appears

3. User Types Message
   └─> Input field accepts text
       └─> Send button enables
           └─> Enter key shortcut works

4. User Sends Message
   └─> Message appears in chat
       └─> Loading indicator shows
           └─> API call executes
               └─> Response appears
                   └─> DSL code displays (if generated)

5. User Reviews DSL Code
   └─> Code appears in viewer
       └─> User can scroll
           └─> Buttons available: Edit, Copy, Download, Apply

6. User Edits Code (Optional)
   └─> Clicks Edit button
       └─> Textarea becomes editable
           └─> User modifies code
               └─> Clicks Save or Cancel

7. User Applies Code
   └─> Clicks Apply button
       └─> Callback triggers
           └─> Code appears in main workspace
               └─> Confirmation message shows

8. User Continues Conversation
   └─> Types follow-up message
       └─> Context maintained
           └─> Iterative refinement
               └─> Updated DSL generated
```

## Color Scheme

```
┌─────────────────────────────────────────────┐
│  Dark Theme Gradient                        │
├─────────────────────────────────────────────┤
│  Background:                                │
│    - Primary: #0f0f1e → #1a1a2e            │
│    - Sidebar: #1e1e2e → #2a2a3e            │
│                                             │
│  Accent Colors:                             │
│    - Primary: #4a4af5 → #6b6bf5 (blue)    │
│    - Success: #26de81 (green)              │
│    - Warning: #ffa502 (orange)             │
│    - Danger: #ff4757 (red)                 │
│                                             │
│  Text Colors:                               │
│    - Primary: #e0e0f0 (light)              │
│    - Secondary: #a0a0c0 (muted)            │
│    - Tertiary: #6a6a8a (subtle)            │
│                                             │
│  Borders:                                   │
│    - Primary: #3a3a5a                      │
│    - Accent: #4a4af5                       │
└─────────────────────────────────────────────┘
```

## Responsive Behavior

```
Desktop (> 768px)
┌────────────────────────────────────────┐
│  [Main Workspace]  │  [Sidebar 450px]  │
│  (Flexible width)  │  (Fixed width)    │
└────────────────────────────────────────┘

Mobile (≤ 768px)
┌────────────────────────────────────────┐
│         [Sidebar Full Width]           │
│  (Slides over main workspace)          │
│  (Can be collapsed with toggle)        │
└────────────────────────────────────────┘
```

## State Management

```
AIAssistantSidebar State:
┌─────────────────────────────────────┐
│  messages: ChatMessage[]            │
│  currentDSL: string                 │
│  isLoading: boolean                 │
│  selectedLanguage: LanguageCode     │
│  isEditing: boolean                 │
│  editedDSL: string                  │
│  isOpen: boolean                    │
└─────────────────────────────────────┘

App State:
┌─────────────────────────────────────┐
│  currentDSL: string                 │
│  appliedDSL: string                 │
└─────────────────────────────────────┘
```

## API Request/Response Flow

```
Frontend Request:
POST /api/ai/chat
┌─────────────────────────────────────┐
│  {                                  │
│    "user_input": "Create a CNN",   │
│    "context": {                     │
│      "current_model": "...",        │
│      "conversation_history": [...]  │
│    },                               │
│    "language": "en"                 │
│  }                                  │
└─────────────────────────────────────┘
           │
           ▼
Backend Processing:
┌─────────────────────────────────────┐
│  1. Parse request                   │
│  2. Translate if needed             │
│  3. Call AI assistant               │
│  4. Generate DSL                    │
│  5. Translate response              │
└─────────────────────────────────────┘
           │
           ▼
Backend Response:
┌─────────────────────────────────────┐
│  {                                  │
│    "response": "I've created...",   │
│    "dsl_code": "network {...}",     │
│    "intent": "create_model",        │
│    "success": true,                 │
│    "language": "en"                 │
│  }                                  │
└─────────────────────────────────────┘
```

## File Structure Tree

```
neural/aquarium/
│
├── 📁 src/ (Frontend Source)
│   ├── 📁 components/
│   │   └── 📁 ai/
│   │       ├── 📄 AIAssistantSidebar.tsx
│   │       ├── 🎨 AIAssistantSidebar.css
│   │       ├── 📄 ChatInterface.tsx
│   │       ├── 🎨 ChatInterface.css
│   │       ├── 📄 MessageBubble.tsx
│   │       ├── 🎨 MessageBubble.css
│   │       ├── 📄 DSLCodeViewer.tsx
│   │       ├── 🎨 DSLCodeViewer.css
│   │       ├── 📄 LanguageSelector.tsx
│   │       ├── 🎨 LanguageSelector.css
│   │       └── 📄 index.ts
│   ├── 📁 services/
│   │   ├── 📄 AIService.ts
│   │   └── 📄 index.ts
│   ├── 📁 types/
│   │   ├── 📄 ai.ts
│   │   └── 📄 index.ts
│   ├── 📄 App.tsx
│   ├── 🎨 App.css
│   ├── 📄 index.tsx
│   ├── 🎨 index.css
│   ├── 📄 react-app-env.d.ts
│   └── 📄 setupTests.ts
│
├── 📁 backend/ (Backend API)
│   ├── 📄 api.py
│   ├── 📄 requirements.txt
│   ├── 📄 __init__.py
│   ├── 🚀 start.sh
│   └── 🚀 start.bat
│
├── 📁 public/ (Static Assets)
│   └── 📄 index.html
│
├── 📦 package.json
├── ⚙️ tsconfig.json
├── 🔐 .env.example
├── 🚫 .gitignore
├── 📄 __init__.py
├── 🚀 start-dev.sh
├── 🚀 start-dev.bat
│
└── 📚 Documentation/
    ├── 📖 README.md
    ├── 🚀 QUICKSTART.md
    ├── 🏗️ ARCHITECTURE.md
    ├── 💡 EXAMPLES.md
    ├── 🔗 INTEGRATION.md
    ├── 🚢 DEPLOYMENT.md
    ├── 📊 PROJECT_SUMMARY.md
    ├── ✅ IMPLEMENTATION_CHECKLIST.md
    └── 🎨 VISUAL_OVERVIEW.md (this file)
```

## Icons & Visual Elements

```
Sidebar Header:
┌─────────────────────────────────────┐
│  AI Assistant               [→]     │  (Toggle button)
└─────────────────────────────────────┘

Language Selector:
┌─────────────────────────────────────┐
│  🌐 Language: English ▾             │
└─────────────────────────────────────┘

Message Bubbles:
┌─────────────────────────────────────┐
│  👤 You:                            │  (User message - blue gradient)
│  Create a CNN for MNIST             │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  🤖 Assistant:                      │  (Assistant - dark background)
│  I've created a CNN model...        │
└─────────────────────────────────────┘

Action Buttons:
┌──────┬──────┬──────┬──────┐
│ ✏️ Edit│ 📋 Copy│ ⬇️ Down│ ✓ Apply│
└──────┴──────┴──────┴──────┘

Loading Indicator:
┌─────────────────────────────────────┐
│  ● ● ●                              │  (Animated typing dots)
└─────────────────────────────────────┘
```

## Animation States

```
Sidebar Open/Close:
[Closed] ─────transform: translateX(100%)─────> [Open]
[Open]   <────transform: translateX(0%)───────── [Closed]

Message Appear:
┌──────┐
│      │ ─┐
│ New  │  │ slideIn animation
│ Msg  │ <┘ (0.3s ease-out)
└──────┘

Button Hover:
[Button] ───hover───> [Button ↑] (translateY(-2px) + shadow)
         <──hover──   [Button]

Loading Dots:
● ○ ○  ───>  ○ ● ○  ───>  ○ ○ ●  ───>  ● ○ ○  (repeat)
```

## Accessibility Features

```
✓ Semantic HTML elements
✓ ARIA labels on buttons
✓ Keyboard navigation support
✓ Focus indicators
✓ Screen reader friendly
✓ Color contrast compliance
✓ Scalable text sizes
✓ Clear visual hierarchy
```

## Browser Compatibility

```
✓ Chrome 90+
✓ Firefox 88+
✓ Safari 14+
✓ Edge 90+
✓ Mobile browsers (responsive)
```

This visual overview provides a comprehensive understanding of the Neural Aquarium interface, architecture, and user experience.
