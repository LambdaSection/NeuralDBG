apiVersion: v1
kind: Secret
metadata:
  name: neural-secrets
  labels:
    app: neural
type: Opaque
stringData:
  # IMPORTANT: Replace these values with actual secrets
  # Generate secret-key with: python -c "import secrets; print(secrets.token_hex(32))"
  secret-key: "REPLACE-WITH-SECURE-SECRET-KEY-MINIMUM-32-CHARACTERS"
  database-url: "postgresql://neural:password@neural-postgres:5432/neural"
  redis-password: ""
