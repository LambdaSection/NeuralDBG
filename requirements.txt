# Core dependencies for Neural DSL
# Install with: pip install -r requirements.txt

# DSL Core
click>=8.1.3
lark>=1.1.5
numpy>=1.23.0
pyyaml>=6.0.1

# For additional features, install with extras:
# pip install -e ".[hpo]"          # Hyperparameter optimization
# pip install -e ".[cloud]"        # Cloud integrations
# pip install -e ".[visualization]" # Visualization tools
# pip install -e ".[dashboard]"    # NeuralDbg dashboard
# pip install -e ".[backends]"     # ML framework backends (TF, PyTorch, ONNX)
# pip install -e ".[full]"         # All optional features
# pip install -e ".[dev]"          # Development tools

# Or install individual dependencies as needed:
# For backends: tensorflow>=2.6, torch>=1.10.0, onnx>=1.10
# For HPO: optuna>=3.0, scikit-learn>=1.0
# For visualization: matplotlib<3.10, graphviz>=0.20, plotly>=5.18
# For dashboard: dash>=2.18.2, flask>=3.0
